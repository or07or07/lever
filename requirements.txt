fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.4.0  # >=3.4.0 fixes CVE-2024-33663 (algorithm confusion) + CVE-2024-33664 (JWE DoS)
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.2,<4.0.0
python-multipart>=0.0.18  # >=0.0.7 fixes CVE-2024-24762 (ReDoS), >=0.0.18 fixes CVE-2024-53981 (DoS)
python-dotenv>=1.0.0
aiofiles>=23.2.1

# TOTP (admin MFA, GP-14)
pyotp>=2.9.0

# Rate limiting (GP-12) — optional: only used if REDIS_URL is configured,
# otherwise rate limiting stays in-memory and this is simply unused.
redis>=5.0.0

# PostgreSQL driver (used when DATABASE_URL starts with postgresql://)
psycopg2-binary>=2.9.9

# Database migrations
alembic>=1.13.0

# Geocoding (Day 60 — search + geolocation)
httpx>=0.27.0

# Push notifications (FCM HTTP v1) — used only when FCM_CREDENTIALS_PATH is
# set; mints the OAuth2 token from the service account. Safe to install
# always; push stays a no-op until credentials are configured.
google-auth>=2.28.0
